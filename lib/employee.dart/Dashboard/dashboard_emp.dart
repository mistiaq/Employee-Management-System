import 'package:flutter/cupertino.dart';

class dashboard_emp extends StatefulWidget {
  const dashboard_emp({Key? key}) : super(key: key);

  @override
  _dashboard_empState createState() => _dashboard_empState();
}

class _dashboard_empState extends State<dashboard_emp> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
